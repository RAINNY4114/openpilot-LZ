# 福特/林肯非CANFD转向优化（最终执行版）

更新时间：2026-03-09

## 1. 目标与结论
- 目标：解决“方向盘力度偏软”，重点是回正慢，同时提升弯中支撑感。
- 结论：当前分支下，主瓶颈是角度控制链路的曲率速率限制（尤其 `DOWN rate`），并且必须和 safety 同步修改。
- 结果：已完成两阶段优化（回正 + 弯中），且仅对非CANFD生效，CANFD保持原标定。

---

## 2. 已落地方案（按执行顺序）

### 阶段1：回正力度优化（已完成）
- 非CANFD `DOWN rate`：
  - `0.00045 -> 0.00055`（低速）
  - `0.00015 -> 0.00020`（高速）
- 目标收益：回正更快，减少“回正发软”。

### 阶段2：弯中力度优化（已完成）
- 非CANFD `UP rate`：
  - `0.00045 -> 0.00050`（低速）
  - `0.00010 -> 0.00011`（高速）
- 目标收益：入弯/持弯支撑增强，弯中不再明显“虚软”。

### 告警质量优化（已完成）
- `steerSaturated` 的 `desired_lateral_accel` 由模型原始曲率改为控制链路目标曲率（`controlsState.desiredCurvature`）。
- 目标收益：降低误报，避免“看起来像饱和但实际上是模型-控制链路偏差”。

---

## 3. 当前生效标定（最终）

### 非CANFD（已增强）
- `UP rate`: `[0.00050, 0.00011]`
- `DOWN rate`: `[0.00055, 0.00020]`

### CANFD（保持原值，不受本次改动影响）
- `UP rate`: `[0.00045, 0.00010]`
- `DOWN rate`: `[0.00045, 0.00015]`

---

## 4. 同步修改范围（已闭坑）
本次均已同步，避免“只改一侧”导致拦包或行为不一致：
- controller
- safety
- safety tests
- selfdrived 告警判定

已闭坑项：
- 坑A：误伤 CANFD（已避免）
- 坑B：只改 controller 不改 safety（已避免）
- 坑C：告警用模型原始目标导致误报（已避免）

---

## 5. 修改文件清单
- `opendbc_repo/opendbc/car/ford/values.py`
- `opendbc_repo/opendbc/car/ford/carcontroller.py`
- `opendbc_repo/opendbc/safety/modes/ford.h`
- `opendbc_repo/opendbc/safety/tests/test_ford.py`
- `selfdrive/selfdrived/selfdrived.py`

说明：`system/athena/serial_whitelist.txt` 的改动是独立改动，不属于本次转向方案核心内容。

---

## 6. 效果预期
- 回正：相对原版明显加快（高速段改善最明显）。
- 弯中：支撑感增强，入弯与持弯不再明显“软”。
- 告警：`Turn Exceeds Steering Limit` 误触发概率下降。

---

## 7. 验证状态
已完成：
- 代码一致性核对（controller/safety/tests 对齐）
- Python 语法编译检查通过（相关 `.py` 文件）

未完成（环境限制）：
- safety 单测全量执行需要 `capnp` 依赖，当前环境缺失。

建议实车验收指标：
- 回正收敛时间（重点看匝道回正段）
- 弯中修正频率与摆动感
- `steerSaturated` 每100km触发次数
- `pandaStates.safetyTxBlocked` 是否增加（预期不增加）

---

## 8. 后续策略（如仍需更强力度）
- 若仍觉得弯中偏软：继续小步提升非CANFD `UP rate`（建议每次 +5% 内），并保持 controller+safety+tests 同步。
- `CURVATURE_ERROR` 暂不建议先动；仅在日志证明长期打满误差窗时再进入第三阶段。

# 模型重启丢失问题 - 修改前后对比

## 问题场景

**测试步骤**：
1. 选择非默认模型（如 tinygrad 模型）并下载完成
2. 连接车辆，设备进入 onroad 状态
3. 重启设备

**期望结果**：模型保持选择
**实际结果（修改前）**：模型回退到默认 stock

---

## 修改前的问题流程

### 1. 保存阶段（正常）
```python
# model_manager.py:308
self.params.put(PARAM_ACTIVE_BUNDLE, self.active_bundle.to_dict())
```

保存的数据格式（capnp to_dict() 输出）：
```json
{
  "runner": "tinygrad",           ← 字符串
  "status": "downloaded",         ← 字符串
  "models": [
    {
      "type": "vision",           ← 字符串
      "artifact": {
        "downloadProgress": {
          "status": "cached"      ← 字符串
        }
      }
    }
  ]
}
```

### 2. 加载阶段（失败）
```python
# model_manager_helpers.py:116 (修改前)
def _coerce_bundle(bundle):
    bundle = _normalize_bundle_dict(bundle)  # 只处理字段名，不处理枚举类型
    try:
        return custom.ModelManagerSP.ModelBundle(**bundle)  # ❌ 抛出异常
    except Exception:
        return None  # ❌ 静默返回 None
```

**失败原因**：
- `_normalize_bundle_dict()` 只转换字段名（snake_case → camelCase）
- **没有转换枚举类型**（字符串 → 整数）
- capnp 构造函数期望 `runner: int`，但收到 `runner: "tinygrad"`
- 抛出 TypeError/ValueError，被静默捕获

### 3. 结果
```python
# process_config.py:89
def is_tinygrad_model(started, params, CP):
    return bool(get_active_model_runner(params, True) == custom.ModelManagerSP.Runner.tinygrad)
    # get_active_bundle() 返回 None
    # get_active_model_runner() 默认返回 stock (2)
    # 2 != 1 (tinygrad)
    # 返回 False
```

**最终**：系统启动 stock runner，模型丢失

---

## 修改后的正确流程

### 1. 保存阶段（不变）
数据格式相同，仍然是字符串枚举

### 2. 加载阶段（修复）
```python
# model_manager_helpers.py:47-52 (新增)
def _normalize_bundle_dict(bundle):
    # ... 原有字段名转换 ...

    # ✅ 新增：转换 runner 枚举
    if "runner" in normalized and isinstance(normalized["runner"], str):
        runner_map = {"snpe": 0, "tinygrad": 1, "stock": 2}
        normalized["runner"] = runner_map.get(normalized["runner"].lower(), 2)
```

转换后的数据：
```json
{
  "runner": 1,              ← 整数（tinygrad）
  "status": 2,              ← 整数（downloaded）
  "models": [
    {
      "type": 2,            ← 整数（vision）
      "artifact": {
        "downloadProgress": {
          "status": 3       ← 整数（cached）
        }
      }
    }
  ]
}
```

```python
# model_manager_helpers.py:116 (修改后)
def _coerce_bundle(bundle):
    bundle = _normalize_bundle_dict(bundle)  # ✅ 转换所有枚举为整数
    try:
        return custom.ModelManagerSP.ModelBundle(**bundle)  # ✅ 成功构造
    except Exception as e:
        cloudlog.error(f"failed to parse bundle: {e}")  # ✅ 记录错误
        return None
```

### 3. 结果
```python
# process_config.py:92
def is_tinygrad_model(started, params, CP):
    return bool(get_active_model_runner(params, True) == custom.ModelManagerSP.Runner.tinygrad)
    # get_active_bundle() 返回有效的 bundle 对象
    # bundle.runner = 1 (tinygrad)
    # get_active_model_runner() 返回 1
    # 1 == 1 (tinygrad)
    # 返回 True
```

**最终**：系统启动 tinygrad runner，模型保持 ✅

---

## 关键修改点

### 1. 添加 runner 枚举转换
```python
if "runner" in normalized and isinstance(normalized["runner"], str):
    runner_map = {"snpe": 0, "tinygrad": 1, "stock": 2}
    normalized["runner"] = runner_map.get(normalized["runner"].lower(), 2)
```

### 2. 添加 Model.type 枚举转换
```python
if "type" in model_out and isinstance(model_out["type"], str):
    type_map = {"supercombo": 0, "navigation": 1, "vision": 2, "policy": 3, "offPolicy": 4, "offpolicy": 4}
    model_out["type"] = type_map.get(model_out["type"], 0)
```

### 3. 添加 status 枚举转换（两处）
```python
# Bundle.status
if "status" in normalized and isinstance(normalized["status"], str):
    status_map = {"notDownloading": 0, "downloading": 1, "downloaded": 2, "cached": 3, "failed": 4}
    normalized["status"] = status_map.get(normalized["status"], 0)

# DownloadProgress.status
if "status" in dp and isinstance(dp["status"], str):
    status_map = {"notDownloading": 0, "downloading": 1, "downloaded": 2, "cached": 3, "failed": 4}
    dp["status"] = status_map.get(dp["status"], 0)
```

### 4. 添加错误日志
```python
except Exception as e:
    cloudlog.error(f"model_manager_helpers: failed to parse bundle: {e}")
    return None
```

---

## 为什么离线正常，在线失败？

### 离线场景（修改前也正常）
- 设备保持 offroad
- `models_manager` 进程持续运行
- 使用 `ModelParser._parse_bundle()` 重新解析
- `ModelParser` 使用字段赋值方式：`model_bundle.runner = runner`
- capnp 的字段赋值会自动尝试类型转换（字符串 → 枚举）
- 每秒刷新，即使失败也会重试

### 在线场景（修改前失败）
- 设备连接车辆后立即 onroad
- `models_manager` 不运行（only_offroad）
- 直接使用 `_coerce_bundle()` 加载
- 使用构造函数方式：`ModelBundle(**bundle)`
- capnp 构造函数**不会**自动转换字符串枚举
- 一次失败，永久回退到 stock

---

## 现在能解决问题吗？

**答案：是的 ✅**

修改后的代码能够：
1. ✅ 正确处理 capnp `to_dict()` 保存的字符串枚举
2. ✅ 在加载时将所有枚举字段转换为整数
3. ✅ 确保 capnp 构造函数能够成功创建 bundle 对象
4. ✅ 在线重启时正确恢复模型选择
5. ✅ 添加错误日志，便于后续调试

**验证方法**：
1. 选择 tinygrad 或 snpe 模型并下载完成
2. 连接车辆，确保设备 onroad
3. 重启设备
4. 检查 `/data/params/d/ModelManager_ActiveBundle` 是否存在
5. 检查日志中是否有 "failed to parse bundle" 错误
6. 确认启动的是正确的 runner（modeld 或 modeld_snpe）
